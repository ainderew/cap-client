export enum keys {
  AUTH_TOKEN_KEY = 'authToken',
  PROFILE = 'profile'
}
export enum AccountType {
  business = 'business',
  customer = 'customer'
}

export enum ResponseRoles {
  ai = 'ai',
  user = 'user'
}
